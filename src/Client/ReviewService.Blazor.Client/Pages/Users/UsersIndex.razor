@page "/users"
@if (_users == null)
{
    <Loading />
}
else
{
    <MudTable Items="@_users" Hover="true" Breakpoint="Breakpoint.None" Class="px-4 mt-8 ml-8 mr-8" Elevation="14">
        <HeaderContent>
            <MudTh>Full name</MudTh>
            <MudTh>Email</MudTh>
            <MudTh>Role</MudTh>
        </HeaderContent>
        <RowTemplate>
            <MudTd DataLabel="Full name">@context.FullName</MudTd>
            <MudTd DataLabel="Email">@context.Email</MudTd>
            <MudTd>
                @foreach (var role in context.Roles)
                    {
                    @role
                    }
            </MudTd>
        </RowTemplate>
    </MudTable>
}
